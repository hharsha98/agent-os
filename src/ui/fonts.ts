// Bundled variable fonts so the desktop app renders correctly offline —
// no network font request ever happens. Both are OFL-licensed.
import "@fontsource-variable/inter";
import "@fontsource-variable/jetbrains-mono";
